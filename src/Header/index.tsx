import { Box } from '@mui/system';

const Header = () => {
  return (
    <Box
      sx={{ width: '100%', height: '76rem', backgroundColor: 'primary.main' }}
    ></Box>
  );
};

export default Header;
